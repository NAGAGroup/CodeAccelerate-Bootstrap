# env.nu is deprecated as of Nushell 0.101+ (this machine runs 0.114). Everything
# that used to live here now lives in config.nu and the autoload/ directory
# (loaded automatically, alphabetically, AFTER config.nu). Kept as an intentional,
# near-empty placeholder so the config dir layout is obvious.
