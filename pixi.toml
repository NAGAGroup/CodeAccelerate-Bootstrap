[project]
name = "dotfiles"
version = "0.1.0"
description = "Add a short description here"
authors = ["Jack Myers <jackhmyers97@gmail.com>"]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tasks]
install-user-tools = "sh $PIXI_PROJECT_ROOT/scripts/install-user-tools.sh"
